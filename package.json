{
  "name": "galenico",
  "private": true,
  "version": "0.0.0",
  "description": "App galeniche Farmacia della Stazione — test infrastructure (l'app gira come single-file index.html, nessun build step in prod)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
